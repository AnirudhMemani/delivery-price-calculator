import expressAsyncHandler from "express-async-handler";
import { ResourceNotFoundError } from "../middlewares/GlobalErrorHandler.js";

export const calculateDeliveryCost = expressAsyncHandler(
    async (req, res) => {}
);
